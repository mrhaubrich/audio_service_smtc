name: audio_service_smtc
description: A Windows implementation of audio service using System Media Transport Controls (SMTC).
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.5.0
  flutter: ^3.24.0

dependencies:
  audio_service_platform_interface: ^0.1.3
  ffi: ^2.1.0
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.8
  win32: ^5.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^6.0.0
